# First export your api key and application key using bash

# Request using your keys
curl -X GET -Is "https://api.datadoghq.com/api/v1/dashboard" -H "Accept: application/json" -H "Content-Type: application/json" -H "DD-API-KEY: ${DD_API_KEY}" -H "DD-APPLICATION-KEY: ${DD_APP_KEY}"
